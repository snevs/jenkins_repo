pipeline {
    agent any

    stages {
        stage('Echo String') {
            steps {
                script {
                    echo 'Hello, this is a simple Jenkinsfile!'
                }
            }
        }
    }
}
